import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Signin = () => {
  return <Text style={{ fontSize: 30 }}>Signin</Text>;
};

const styles = StyleSheet.create({});

export default Signin;
