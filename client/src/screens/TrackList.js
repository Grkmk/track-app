import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrackList = () => {
  return <Text style={{ fontSize: 30 }}>TrackList</Text>;
};

const styles = StyleSheet.create({});

export default TrackList;
