import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const Signup = () => {
  return <Text style={{ fontSize: 30 }}>Signup</Text>;
};

const styles = StyleSheet.create({});

export default Signup;
