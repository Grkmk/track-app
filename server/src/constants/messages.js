const mustLogin = 'You must be logged in.';
const noCredentials = 'Must provide email and password';
const invalidCredentials = 'Invalid password or email';
const noNameOrLocation = 'You must provide a name and location';

module.exports = {
  mustLogin,
  noCredentials,
  invalidCredentials,
  noNameOrLocation
};
